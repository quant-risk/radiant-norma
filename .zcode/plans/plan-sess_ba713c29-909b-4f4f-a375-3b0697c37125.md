## Plano de Fechamento — Sprint 57 Generator + Testes + Wizard Backward

### Visão geral

Levar o motor de geração (Sprint 57) a 100% funcional, adicionar testes onde estão zerados, e permitir navegação reversa no wizard. Tudo respeitando o plano mestre (ROADMAP.md) e o que já está construído.

---

### Fase 1 — Generator Registry auto-registro (CRÍTICO)

**Problema real:** `generator.NewRegistry()` retorna registry vazio. `Registry.Get("3040")` retorna nil em produção.

**Arquivos:**
- `backend/internal/generator/interface.go` (linhas 161-171 — `NewRegistry()` vazio)
- `backend/internal/generator/gen*/generator.go` (10 packages sem `init()`)

**Ação:**
1. Criar `backend/internal/generator/generate.go` com a função `init()` que importa blank todos os 10 subpacotes (`gen3040`, `gen3050`, `gen4111`, `gen2030`, `gen2060`, `gen2061`, `gen2062`, `gen2070`, `gen2160`, `gen2170`).
2. Em cada subpacote, adicionar `func init() { registry.Default.Register(New()) }` (usa registry package-level).
3. Ajustar `NewRegistry()` para também popular via `Default` se estiver vazio.

**Validação:** Teste que `registry.Get("3040")` retorna gerador não-nil em runtime.

---

### Fase 2 — Adapters Manual + File (completar 5/5)

**Arquivo:** `backend/internal/generator/adapters/adapters.go`

**Adicionar:**
1. `ManualAdapter` — recebe `CanonicalDocument` via payload JSON injetado pelo wizard frontend. Implementa `Fetch(ctx, source)` retornando o doc injetado.
2. `FileAdapter` — recebe XLSX/CSV de uploads wizard. Parser para XLSX (já tem `excelize/v2` no go.mod). Para CSV: split linhas em `Operacao`. 

**Estratégia simplificada (sem perder tempo):**
- `ManualAdapter` — Stub funcional: recebe `map[string]any` da request, retorna `CanonicalDocument` parseado.
- `FileAdapter` — Stub funcional: lê arquivo via interface `FileReader`, parseia primeira linha como header CSV/XLSX, retorna lista vazia com erro explícito se formato desconhecido. **Comentário: substituir por parser real quando integração com wizard estiver pronta.**

---

### Fase 3 — Wirar ValidateBatch no batch handler

**Problema:** `validation.ValidateBatch` definido mas nunca chamado.

**Arquivo:** `backend/internal/api/generate.go` (handler `/v1/generate/batch`)

**Ação:** Após gerar N CADOCs, coletar XMLs gerados e chamar `ValidateBatch(ctx, xmlDocs, engine)`. Se L4 cross-doc falhar, retornar 422 com detalhes da regra violada.

---

### Fase 4 — Synth-gen: expandir CadocType enum (3 → 10)

**Arquivo:** `backend/internal/synth/generator.go`

**Ação:** Adicionar constantes `Cadoc2030`, `Cadoc2060`, `Cadoc2061`, `Cadoc2062`, `Cadoc2070`, `Cadoc2160`, `Cadoc2170` ao enum. Para cada um, escrever grounding prompt mínimo (1-2 frases descrevendo o CADOC e suas tabelas principais). Seguindo o template do `Cadoc3040`.

---

### Fase 5 — Testes críticos zerados

**Criar:**
1. `backend/internal/api/sta_range_handlers_test.go` — table-driven tests para `staRangeInit`, `staRangeUpload`, `staRangeStatus`. Cobrir 400 (param inválido), 500 (STA client não é RangeUploader), 200 (sucesso com mock RangeUploader).
2. `backend/internal/crossdoc/rules/xd_rules_test.go` — testar cada uma das 9 regras XD02-XD12 com DocSets mínimos. Verificar findings (sem erro quando consistente, com erro quando violado).

---

### Fase 6 — Wizard backward transitions

**Arquivo:** `backend/internal/generator/wizard/session.go`

**Ação:**
1. Adicionar `func (s Step) Prev() Step` — retorna o estado anterior (linear reverso).
2. Adicionar método `Store.Revindicate(ctx, sessionID, data) → *Session` — volta ao step anterior, preservando `data` acumulado.
3. Atualizar API handler `advanceWizard` para aceitar opcionalmente `?direction=prev` na request.
4. Documentar limitação: dados do step que está "voltando" são limpos (idempotente — usuário precisa preencher de novo).

---

### Fase 7 — Validação + commits granulares

1. `go build ./...` em cada fase — confirmar compilação.
2. `go test ./...` — rodar suite completa.
3. **Commit por fase** — cada fase vira um commit separado no `main` (facilita review + reversão se algo quebrar).
4. Atualizar `CHANGELOG.md` com entrada `v3.36.3 — Sprint 57 fechado a 100%` ao final.

---

### O que NÃO farei nesta sessão

- Frontend wizard UI (Next.js) — bloqueado por iCloud File Provider deadlock, fora de escopo.
- LLM streaming real (substituir word-delay) — depende de mudar API calls; só faço se sobrar ciclo.
- `PilotService` DB integration tests — cobertura baixa mas funciona em runtime.

---

### Critério de "100%"

Sprint 57 estará fechado quando:
- ✅ `registry.Get(cadoc).Generate(...)` funciona para todos 10 CADOCs sem nil.
- ✅ 5/5 adapters retornam `CanonicalDocument` (3 reais, 2 funcionais com parser mínimo).
- ✅ Batch handler chama ValidateBatch e retorna 422 em cross-doc violation.
- ✅ Synth-gen suporta os 10 CADOCs.
- ✅ Wizard permite `?direction=prev`.
- ✅ 2 suites de teste críticas adicionadas.
- ✅ `go test ./...` passa.
- ✅ CHANGELOG atualizado.
