# macOS
.DS_Store
.AppleDouble
.LSOverride

# Build temporário (Pandoc gera e remove)
_build_*.html

# Logs temporários
*.log
!.gitkeep

# Backup
*.bak
*.tmp

# IDE
.vscode/
.idea/
*.swp

# Python
__pycache__/
*.pyc
.venv/
venv/

# Não commitar catálogo grande (regenerável)
# Mas como tá pequeno (1.7 MB) e é insumo do Norma Audit, mantemos
# _catalogos/criticas.json
# _catalogos/leiautes.json

# Não commitar arquivos grandes > 50 MB (PDFs pesados, etc.)
# Mas até agora todos < 10 MB

# Backend runtime — gerados, não commitar
backend/radiant.db
backend/radiant.db-shm
backend/radiant.db-wal
*.db
*.db-shm
*.db-wal
backend/api
backend/seed
backend/worker

# Backend Go
backend/vendor/

# Materiais que podem ser baixados novamente
_catalogos/_extracao.log